# Per-user zsh settings. The NyxOS defaults (prompt, completion, history,
# plugins) live in /etc/zsh/zshrc and are loaded before this file; anything
# set here overrides them. The file exists so zsh's first-run wizard
# (zsh-newuser-install) never appears.
