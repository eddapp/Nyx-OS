[Unit]
Description=NyxOS clipboard auto-clear (X11 CLIPBOARD selection, 45s idle timeout by default)
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/nyx-clipboard-clear
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
