[package]
name = "nyx-browsers"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["fs", "user"] }
nyx-core = { version = "0.1.0", path = "../nyx-core" }
tracing = "0.1.44"
